#include <stdio.h>
/**
 * main - entry of the program
 * @num - variable to be used
 *
 * Return: 0 means success
 */

int main(void)
{
	for (int num = 0; num < 10; num++)
	{
		printf("%d\n", num);
	}
	return (0);
}
